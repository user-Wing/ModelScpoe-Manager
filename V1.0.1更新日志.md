# **更新日志 V1.0.1**

**a. 界面导航与布局重构**  
- 左侧导航栏增加三横线收起/展开按钮，收起后仅保留图标及悬停提示；版本号居中显示，设置图标与文字间距统一。  
- 资源管理页移除标题、副标题及页内筛选栏，搜索独立为非模态窗口（支持多条件筛选和历史记录）。  
- 右侧浏览改为单栏详细信息视图（名称、类型、大小），支持按列排序和分组（`<1MB`、`1MB-1GB`、`>1GB`）。  
- 顶部新增“查看”模式切换（详细/缩略图）、紧凑视图及新建文件夹、下载、网页管理入口；读取/刷新/更新索引合并为三个横向按钮。  
- 修复侧栏三横线位置及导航控件Y坐标，收起后保留品牌与版本占位高度。

**b. 缩略图策略全面升级（性能与体验）**  
- 改为递归闲时队列生成：当前目录优先，大仓库（>100项）每批4项、2线程、间隔900ms，小仓库每批16项、4线程；用户交互（键盘/鼠标/滚轮）推迟下一批，失败项不会无限重试。  
- 缩略图模式改为方形图标网格（图标下方显示名称），画框固定208×117（16:9），卡片224×154，统一等比缩放补边。  
- 进入目录即自动后台处理全部直属媒体，不再需先切换模式；仅对视口项目及相邻预取范围、停止滚动250ms后生成，快速滚动中断旧批次。  
- 视频缩略图使用FFmpeg从第0秒提取首帧（限制探测数据），图片以内存流处理，最终缩略图永久缓存于`data/thumbnails`，不影响文件移动/删除。  
- 所有FFmpeg子进程隐藏窗口、`-hide_banner`和`-loglevel error`。**便携包中不含ffmpeg，需要自行添加到系统path。**

**c. 大目录流畅浏览与排序修复**  
- 左侧目录树不再为每个文件创建节点，仅构建目录；右侧结果按路径缓存，详细模式不创建隐藏缩略图项目，批量填充时暂停重绘。  
- 彻底移除自定义`QTreeWidgetItem.__lt__`，避免Qt排序回调触发Python虚方法导致进程崩溃；现改为填充前Python层排序，大小按真实字节数。  
- 直属目录项严格匹配`当前目录/`前缀，修复根目录随机残留（如`root.bin`误显示为`in`），新增独立可测试的过滤逻辑`_direct_remote_entries`。  
- 文件夹大小显示依赖持久化索引，无缓存显示`--`，索引完成后刷新；进入目录不再把浏览结果写入索引。

**d. 跨仓库复制/粘贴与上传下载队列**  
- 右键新增“复制/粘贴”：复制来源可为账户或Public仓库，粘贴到可写目录时，小于阈值资源立即后台下载→上传→清理临时目录；大于阈值先确认。文件夹复制仅更新选中目录的大小索引，不全仓库刷新。  
- 上传改为逐项顺序队列，每项独立目标路径且可编辑；状态列显示百分比、完成、失败、取消或跳过，支持暂停/恢复/取消（取消不删除已上传内容）。  
- 搜索窗口结果也支持右键预览、下载、复制及链接/标签操作；复制文件夹从缓存获取同仓库完整条目集合。

**e. 认证、Public仓库管理与标签系统**  
- 修复认证状态链路：移除资源页标题时误删状态承载控件导致“正在验证令牌”永久停留，现改为不可见状态承载，认证成功后下一事件循环再开始读取。  
- 新增只读`Public`根节点，搜索成功的公开仓库保存本地条目和文件夹大小索引；后台默认不自动更新，仅手动“更新索引”刷新。  
- 公共仓库右键增加“移除/打开网页端”，移除同步清理公开池、本地条目、标签和大小索引。  
- 新增本地多标签表，资源管理顶栏可筛选标签；右键“标签”菜单新建或切换；完整条目缓存删除远端已消失路径的标签映射。

**f. 原生亚克力支持与主题设置**  
- 主题设置新增GPU加速和Blur亚克力半透明开关：GPU开关在下次启动前选择Qt桌面OpenGL或软件OpenGL；亚克力使用Windows 11 DWM或Windows 10 `SetWindowCompositionAttribute`，失败回退普通半透明。  
- 浅色/深色主题及“跟随系统”选项，默认跟随并监听系统颜色变化；`QTimeEdit`纳入主题样式，修复深色下限速时间不可见。  
- 设置页重排为基本、主题、账号、下载（含aria2-next）、播放、WebDAV、索引和预览，原有开机自启、限速、PotPlayer、复制阈值等功能保留。  
- WebDAV启动前探测端口，冲突时自动选择相邻端口并显示提示；设置页增加“本页内容自动保存”说明。

**g. 搜索历史与窗口焦点优化**  
- 搜索历史增加单项删除和清空，历史窗口改为无主窗口的非模态窗口，避免覆盖主窗口。  
- 搜索结果支持列头排序，大小按真实字节数；独立搜索窗口第一行提供全部仓库、类型、标签等条件。

**h. 性能监控与内存优化**  
- 大目录切换和按列排序仅处理当前目录项目，离屏构建目录树0.005秒、右侧渲染0.008秒、排序0.007秒（测试数据）。  
- 缩略图永久缓存，视频首帧生成成功并写入本地，避免重复生成。

**i. 修复关键崩溃与路径错误**  
- 移除`QTreeWidgetItem.__lt__`自定义排序，解决便携版PySide6排序回调导致闪退。  
- 修复跨目录路径残留（`folder`内误显示`root.bin`为`in`），新增回归测试覆盖。  
- 修复移除`repo_heading`状态控件导致的认证永久卡住问题。

**j. 其他体验增强**  
- 预览设置新增“自动对大小小于…MB的图片生成缩略图”，视频不受图片大小阈值限制。  
- 复制文件直链：公开仓库构造`https://modelscope.cn/.../resolve/master/...`；私有构造不含Token的API链接，并明确提示需仓库权限。  
- 详细/缩略图空白区域增加右键“粘贴”（只读或无可复制来源时禁用）；左侧目录树文件夹右键复用既有文件菜单。  
- 预览大小改用“增大/减小”按钮，侧栏三横线居中等细节调整。 
