# ModelScope Manager V1.0.2 更新日志

## 主要更新

- 设置页升级为 Fluent Design 风格，恢复中英文、浅色/深色/跟随系统主题和关闭窗口询问，并修复下拉菜单在浅色模式出现黑块或黑影的问题。
- 账号、下载、播放、索引和 WebDAV 等复杂设置改为统一居中的折叠卡片，加入 180ms 缓动展开/收起动画；深色模式下的添加、移除按钮恢复为清晰的 Fluent 图标。
- 修复 WebDAV 未占用端口被误报为“未找到端口”的问题，端口冲突时会自动寻找相邻可用端口并保存实际值。
- 缩略图生成提高并发与响应速度；视频改为优先截取约 1.5 秒位置，减少首帧黑屏，并将缓存源图提高到 320×180。远端视频继续使用 FFmpeg 输入前快速定位，尽量利用 HTTP Range 减少读取量。
- 图床按账户记住上次使用的仓库。
- 搜索移除 500 项截断，支持名称、路径、扩展名、类型、引号、多关键词 AND 和通配符等 Everything 风格高级搜索，并恢复名称、类型、大小、仓库和路径排序。
- 优化大目录浏览、缩略图队列和搜索防抖；默认策略会在工作期间使用更多 CPU、网络、磁盘和 FFmpeg 并发，以换取更快的预览和搜索反馈。

## 使用说明

- 完整便携包解压后运行 `start.bat`，无需额外安装 Python 依赖。
- 若缩略图生成占用过高，可在“设置 → 索引和预览”中降低缩略图线程数。
- 搜索结果依赖本地索引；远端仓库尚未完成分页索引时，应先读取仓库或更新索引。
