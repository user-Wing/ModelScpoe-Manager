# ModelScope Manager V1.0.3 更新日志

## 主要更新

- 新增多网页登录账户：可在“设置 → 账号”中通过 `+` 添加账户名称并打开 ModelScope 官方页面登录，成功后会显示“成功”。网页登录会话使用 Windows DPAPI 与当前设备绑定加密，不以明文写入配置或日志。
- 资源管理器现在区分 Public、Token 登录账户和网页登录账户。Public 保持只读；Token 账户继续使用稳定的官方 SDK 进行读取、上传和秒传；删除、移动和重命名由网页登录账户执行。
- 新增程序内删除。删除前始终显示不可逆确认；删除文件夹时会递归处理全部文件，并将最多 100 个文件合并为一次提交。请求结果不明确时只查询目标目录核对实际残留，避免服务端已成功但客户端误报失败。
- 新增移动和区分大小写的重命名。操作会先经过下载阈值确认，将内容下载到临时目录，并使用 Token SDK 上传到新路径；只有全部上传成功后才删除旧路径，上传失败时保留原文件。
- 删除完成后改为增量更新 SQLite 条目、标签和目录大小，只刷新当前目录，不再重新扫描整个仓库，改善大目录删除后的等待时间与索引一致性。
- 修复深色模式下上传百分比文字对比度过低；调整 Token、播放器加减按钮对齐，增加账号状态与分割线以及“下载与传输”设置项的纵向间距。
- 设置页下拉菜单改为无动画显示并在关闭时重绘，修复透明菜单残影。

## 使用与安全说明

- 网页登录仅用于身份校验、读取、下载和删除；上传、复制、移动及重命名的新路径写入仍要求目标仓库可用的 Token 账户，并统一走官方 SDK。
- Public 和 Token 账户中的删除、移动、重命名菜单会保留但不可点击，并说明只读原因或提示改用在线登录账户。
- GitHub 源码不包含 `data/`、`runtime/`、`dist/`、本地 Cookie、Token、数据库、缩略图或其他运行缓存。升级源码不会主动清除本机已有账户和网页登录信息。
