# ModelScope Manager V1.0.4 更新日志

## 1. 安全更新

- **认证信息重定向边界**：新增 `http_security.py`，Token/Cookie 只会主动发送给 ModelScope 官方域；`urllib` 跨 origin 重定向会移除 `Authorization / Cookie / Proxy-Authorization`，同时保留 Range。已接入缩略图、复制下载、WebDAV、媒体代理和普通下载链。
- **网页登录 Cookie 作用域**：`web_session.py` 不再使用 domainless 的 `cookies=dict`；统一建立 `requests.Session`，Cookie 明确绑定 `.modelscope.cn`。在线登录、用户名验证、单文件删除、批量删除和删除后的目录核对接口保持原功能。
- **aria2 Token 落盘问题**：移除了包含 Token 的 `.aria2-input` 临时 manifest。现在 aria2 启动后通过仅监听 `127.0.0.1` 且带随机 RPC secret 的 JSON-RPC `addUri` 注入每个下载任务及其认证头，仍保留分段、断点续传、SHA-256、暂停/恢复和动态限速。
- **上传/备份目录越界**：新增 `local_paths.py`，普通上传和备份都会拒绝 symlink / Windows junction，并在 SDK 读取前验证实际文件仍处于用户选择的根目录内。
- **SDK monkey-patch 并发问题**：上传进度对 `modelscope_hub._upload.tqdm` 的临时替换现在受进程级 `RLock` 保护；普通上传、备份、图床和 WebDAV SDK 写入共享这把锁，避免并发线程互相覆盖全局 `tqdm`。
- **DPAPI 降级问题**：Token 和网页登录 Session 的新保存默认只允许当前用户 DPAPI，不再静默退化到 machine scope；`device.id` 和内网 WebDAV 密码保留兼容性 machine fallback。旧 `m:` 凭据仍能读取，并尝试无损迁移到 user scope；迁移失败不会删除仍可使用的旧凭据。
- **DPAPI 不可用时功能回退**：Token 仍可连接但仅本次运行保存在内存；网页登录 Session 同样可以在当前进程继续用于读取和删除，只是不持久写盘。
- **WebDAV 保持内网设计**：没有引入 HTTPS、自签证书或改变 AList 的 `http://` 挂载方式。增加最多 2 个并发 PUT、本机临时空间预留和至少 512 MiB 空闲空间保护，降低 AList 重试/并发把系统盘临时目录写满的风险。
- **WebDAV 多账户路由**：同一仓库由多个账户可见时，写操作会优先选择真正拥有 Token 的 SDK service，不会误选明确禁止上传的网页登录 service。
- **未知 visibility**：不再因为“没有 Token”就把未知可见性推断成 public；未知状态现在 fail-closed。
- **备份失败调度**：数据库新增 `last_attempt / last_success` 并带兼容迁移。失败不再被标记成一次成功扫描，也不会等完整备份周期才重试，而采用 30 秒～5 分钟退避。
- **备份 TOCTOU**：上传前后都会重新核对 size/mtime；扫描后或上传过程中发生变化的文件不会错误写入“已备份”状态。
- **增量备份恢复**：没有改变 `YYYYMMDD-HHMMSS` 增量目录格式，也没有改变“同路径覆盖”模式。现在利用原有 `backup_files` 索引记录每个当前文件最后成功上传的位置，从多个时间戳增量中重建**当前完整文件集合**；老任务缺少这份映射时仍回退到原来的“最新时间戳目录”逻辑。

## 2. 功能更新

- 修复单个文件下载时，下载页面“本地位置”列显示不清楚的问题。
- 修复下载任务状态不更新的问题；Windows 路径写法存在差异时，也能正确找到并更新对应任务行。
- 修复下载刚开始时总进度条立即显示 100% 的问题。下载和校验进行中最多显示 99%，全部完成后才显示 100%。
- 修复已有任务正在上传时，继续拖放文件或文件夹可能无法追加到上传队列的问题。同一仓库的新任务会保持“等待”，并在当前任务结束后继续上传。

## 版本信息

- 程序版本更新为 `1.0.4`。
