# ModelScope Manager V1.0.5 更新日志

## 新增

- 新增传输统计分析页面，展示实时上传/下载速度、会话流量、平均速度、峰值及时间范围图表。
- 新增上传速度学习与持续慢速自动重连机制；文件夹上传支持多文件并发，并隔离每个任务的进度统计。

## 优化与修复

- 优化大型数据集目录读取，将 ModelScope 文件列表分页从每页 100 条提升到接口支持的 3000 条；修复 Token、资源搜索和 WebDAV 打开大型数据集时长时间无法加载的问题。
- 修复 WebDAV Basic Auth 重试时残留 `PROPFIND` 请求体破坏 HTTP 连接的问题。
- 新增 `/dav/` WebDAV 入口并保留原根地址兼容；支持将 `/models`、`/datasets`、`/public` 等子目录作为挂载根目录。
- 优化设置页展开、下拉菜单和主题切换后的残影问题。
- 补全新增界面及状态文本的英文翻译。

## 版本信息

- 程序版本更新为 `1.0.5`。
