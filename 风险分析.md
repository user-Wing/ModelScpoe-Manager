基于 AgentDock 对该目录 Python 代码的首轮静态检查，先列出 5 处较值得优先处理的风险设计。这里区分“已确认的代码行为”和“需要特定条件才能触发的风险”。

| #    | 风险                                                         | 等级     | 主要位置                                                     | 分析                                                         |
| ---- | ------------------------------------------------------------ | -------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 1    | **aria2 临时清单明文保存 Token / Session**                   | 高       | `download_service.py:313-335`                                | `_write_manifest()` 创建 `%TEMP%` 下的 `.aria2-input` 文件，然后直接写入 `header=Authorization: Bearer {token}` 和 `header=Cookie: m_session_id={token}`。正常退出会在 `306-310` 删除，但程序崩溃、强杀、断电时可能残留；同一用户权限下的其他进程也可能在下载期间读取。认证凭据还被复制了两份。建议避免把长期凭据落盘，至少使用短期凭据、严格 ACL，并设计启动时残留清理机制。 |
| 2    | **WebDAV 可暴露到 LAN，但使用明文 HTTP + Basic Auth**        | **严重** | `app.py:2121-2124, 4489-4494`；`webdav_server.py:69-80, 374-390` | UI 明确允许 `0.0.0.0`，生成的地址又固定为 `http://...`。服务端是普通 `ThreadingHTTPServer`，没有 TLS；认证方式只是 `Authorization: Basic base64(username:password)`。因此只要选择“局域网 / Docker”，同网段窃听者就可能获得 WebDAV 用户名密码，同时 GET/PUT 的内容也没有传输加密。建议 LAN 模式强制 TLS，或者仅监听 localhost，把 TLS/鉴权交给反向代理。 |
| 3    | **部分 `urlopen()` 下载路径可能在跨域重定向时泄漏 Authorization/Cookie** | **严重** | `webdav_server.py:462-471`；`app.py:447-448, 499-501`；对照 `media_proxy.py:120-131` | 这是目前比较明确的不一致安全设计。`media_proxy.py` 已专门实现 `_SafeRedirectHandler`：跨域跳转就移除 `Authorization` 和 `Cookie`，注释甚至明确说明是为了 signed-storage redirect。但 WebDAV 和部分 GUI 下载却直接调用默认 `urlopen(Request(...))`。我同时通过 AgentDock 检查了当前 Python 的 `HTTPRedirectHandler.redirect_request()`：它**会复制原请求的所有 header，仅排除 Content-Type/Content-Length**，因此 Authorization/Cookie 也会被复制。若 ModelScope URL 从官方域跳到对象存储域，则认证信息可能被发往目标域。建议把所有带认证头的 urllib 请求统一走 `_SAFE_OPENER` 或公共安全请求封装。 |
| 4    | **WebDAV PUT 可造成临时磁盘耗尽 / 线程资源耗尽**             | 高       | `webdav_server.py:340-371, 492-510`                          | `PUT` 完全相信客户端提供的 `Content-Length`，然后先把整个请求写进 `NamedTemporaryFile`，完成后才上传 ModelScope。Model 仓库只限制到 **50 GB**，Dataset 仓库则没有这里的文件大小上限；同时使用 `ThreadingHTTPServer`，也没有看到全局并发 PUT 数、磁盘预算、请求速率或总临时空间限制。一个认证客户端即可并发制造多个巨型临时文件。建议设置单文件上限、全局临时空间配额、上传并发 semaphore，并最好采用流式/分块上传。 |
| 5    | **备份扫描没有显式阻止符号链接，可能越过备份根目录读取文件** | 中高     | `backup.py:127-141`；`app.py:829-848`                        | `scan_changes()` 使用 `root.rglob("*")` → `path.is_file()` → `path.stat()`，均没有拒绝 symlink/reparse point；之后 `BackupThread` 直接把 `item.path` 交给 `upload_file_as()`。因此，如果备份目录内存在一个指向目录外敏感文件的文件符号链接，扫描看到的是“根目录内的相对路径”，实际读取时却可能跟随链接读取根目录外文件并上传。下载路径部分已经有 `resolve()+is_relative_to()` 边界检查，这套保护在备份上传路径上缺失。建议扫描时拒绝 `path.is_symlink()`，并在上传前用 `resolved_path.is_relative_to(resolved_root)` 再做一次最终边界验证。 |

其中我建议优先处理顺序是 **#3 → #2 → #1 → #4 → #5**。特别是 #3，不只是理论上的“urllib 可能不安全”：项目自身的 `media_proxy.py` 已经针对完全相同类别的重定向做了安全处理，但另外两条调用链遗漏了这一层，属于典型的**安全策略实现不一致**。

这只是第一轮 5 处；尚未展开检查的重点还包括 `app.py` 近 6800 行单文件造成的职责耦合、全局 SDK monkey-patch 的线程安全、WebDAV 暴力破解防护、备份一致性/TOCTOU、数据库并发，以及网页登录 Session 的生命周期管理。



按你补充的约束，上一轮关于 WebDAV 的判断需要调整：**“内网 HTTP + Basic Auth”本身不再作为主要缺陷**。如果它明确定位为“受控 LAN 内 AList 挂载入口”，而且不面向公网，那么证书部署复杂度确实可能大于收益。真正需要关注的是，代码有没有把“受控内网”这一假设强制成边界，而不是仅靠用户记得正确配置。

### 先修正上一轮 5 项

1. **aria2 临时清单明文 Token：仍成立，优先级高。**
   `download_service.py:313-335` 把 Token 写进 `%TEMP%\modelscope-manager-*.aria2-input`。这个风险和 WebDAV 是否内网无关。正常结束会删除，但异常退出可能残留。

2. **WebDAV HTTP + Basic Auth：从“严重漏洞”降为“设计约束/误配置风险”。**
   在可信家庭/实验室 LAN 内，这种方案可以接受。建议不一定上 HTTPS，而是强化边界：

   - 默认仍保持 `127.0.0.1`；
   - 切换 `0.0.0.0` 时给出一次明确提示；
   - 最好允许配置允许访问的 CIDR/IP；
   - 若密码为空或过弱，不允许 LAN 监听；
   - 文档明确“不应通过端口映射、UPnP、FRP、公网反代直接暴露”。

   这样比硬塞一套自签 HTTPS 更符合这个项目的用途。

3. **认证头跨域重定向泄露：仍成立，而且比 #2 更值得修。**
   `media_proxy.py:120-131` 已经有 `_SafeRedirectHandler`，但 `webdav_server.py:462-471` 和 `app.py` 若干直接 `urlopen()` 调用没有用它。Python 默认 `HTTPRedirectHandler` 会复制 Authorization 等普通 header。这个风险发生在 **ModelScope → 对象存储** 的出站链路，与 WebDAV 是 LAN 还是公网无关。

4. **WebDAV PUT 资源消耗：保留，但降为中等。**
   在受控 LAN 下它主要是“误操作/故障客户端导致本机磁盘爆掉”，而不是互联网 DoS。仍建议做总临时空间和并发限制，因为 AList 本身可能重试或并发上传。

5. **备份 symlink 越界：仍成立。**
   这是本地文件安全边界问题，与 WebDAV 无关。

------

## 继续检查后，新发现的重点问题

### 6. DPAPI 会静默降级到 machine scope

**等级：高，建议明确设计选择**

位置：

```
security.py:42-63
for prefix, flags in (("u:", 1), ("m:", 5)):
```

代码先尝试当前用户 DPAPI，失败后自动使用 machine protection。问题不是 `CRYPTPROTECT_LOCAL_MACHINE` 本身不安全，而是它改变了安全模型。

项目的界面和其他代码使用的是：

> “设备绑定加密”
> “安全记住”

但 machine-scope DPAPI 实际意味着同一 Windows 机器上的其他安全主体，在满足文件访问条件时，理论上也能解密。与此同时：

```text
data/settings.ini
data/manager.sqlite3
data/device.id
```

都位于程序目录旁的 portable `data/` 中。

因此当前设计实际是：

> User DPAPI 优先；失败时退化为 Machine DPAPI + 文件权限保护 + device.id 逻辑绑定。

而不是严格意义上的“仅当前用户”。

**建议：**

不要静默降级。至少记录并在 UI 显示：

```text
凭据保护：当前用户
```

或：

```text
凭据保护：本机级（兼容模式）
```

更严格的话，Token/Web Session 不应允许 machine-scope fallback，WebDAV 密码可以允许。

------

### 7. “device binding”主要是逻辑绑定，并不是额外密码学绑定

**等级：中**

`database.py:303-312`：

```python
if bound_id != self.device_id:
    invalid_ids.append(...)
else:
    token = unprotect(cipher)
```

`device_id` 本身又保存在：

```text
data/device.id
```

只是它自身也经过 DPAPI。

因此其安全价值主要是：

- 检测整个 `data` 目录被复制到另一台机器；
- 检测 device.id 丢失/变化；
- 自动销毁旧 Token。

这个设计对“便携程序目录被复制到别的电脑”非常有效。

但它**不能真正进一步约束 DPAPI 解密权限**，因为：

```python
protect(account.token)
```

没有把 `device_id` 作为 DPAPI optional entropy。

所以“device binding”目前是：

```text
解密成功
    ↓
比较数据库里的 device_id
```

而不是：

```text
DPAPI(ciphertext, entropy=device_id)
```

这不是漏洞，但建议代码/文档把它称作：

> 设备身份一致性绑定

而不是暗示 device ID 本身参与了加密密钥派生。

如果希望成为真正的密码学绑定，可以给 `CryptProtectData` 的 optional entropy 参数传入 device secret。

------

### 8. 上传进度实现通过 monkey-patch 修改第三方 SDK 的全局 `tqdm`

**等级：中高，属于架构风险**

`service.py:274-289`：

```python
original_tqdm = upload_module.tqdm

def progress_factory(...):
    return _CallbackTqdm(...)

upload_module.tqdm = progress_factory
try:
    yield
finally:
    upload_module.tqdm = original_tqdm
```

这是**进程级全局状态修改**。

当前普通上传队列一次只运行一个 `UploadThread`，所以主路径暂时降低了触发概率。但项目还有独立的：

- `BackupThread`
- `ImageUploadThread`
- WebDAV 上传线程
- SDK 内部 worker threads

今后只要另一路在这个 context 存活期间调用了 SDK 上传代码，就可能使用错误的 `tqdm` 工厂。

更微妙的是两个并发 context：

```text
线程 A: original = real_tqdm
线程 A: tqdm = callback_A

线程 B: original = callback_A
线程 B: tqdm = callback_B

线程 A: finally -> tqdm = real_tqdm
线程 B: finally -> tqdm = callback_A
```

最后全局状态可能永久残留成 `callback_A`。

这个属于典型的**并发环境下 monkey-patch 不可组合**问题。

建议最终移除动态替换，或者至少：

```python
_UPLOAD_PROGRESS_PATCH_LOCK = threading.Lock()
```

整个 context 独占。

不过锁只缓解，并不是理想解决方案。

------

### 9. `configure_upload_limit_supplier()` 同样永久 monkey-patch SDK 私有实现

**等级：中高**

`service.py:21-57`：

```python
import modelscope_hub._upload as upload_module

stream_class = upload_module._CountedReadStream
...
stream_class.read = limited_read
```

以及：

```python
manager_class = upload_module.UploadManager
...
manager_class._build_operation = limited_build_operation
```

这里有三个风险。

第一，直接依赖：

```python
modelscope_hub._upload
_CountedReadStream
UploadManager._build_operation
```

全是**私有实现**。SDK 小版本升级都可能破坏。

第二，这是永久的进程级修改，无法按某个 `ModelScopeService` 隔离。

第三：

```python
_UPLOAD_LIMITER
```

本身又是一个模块级全局对象。因此所有账户、WebDAV、备份、图片上传共享同一个限速语义。

如果这是“全局总上传带宽限制”，语义上合理；但实现层最好不要依赖 SDK 私有 monkey patch。

------

### 10. 当前备份模式存在明显的一致性问题

**等级：高，功能风险比安全风险更实际**

`BackupThread.run()`：

```python
changed, oversized = self.store.scan_changes(self.job)
...
for item in changed:
    self.service.upload_file_as(...)
    self.store.mark_uploaded(...)
...
self.store.mark_scan(...)
```

`scan_changes()` 判断变化依据只有：

```python
(size, mtime_ns)
```

存在 TOCTOU：

```text
扫描：
file.bin = 10 MB, mtime=A

之后用户修改 file.bin
    ↓
上传读取的是新内容
    ↓
mark_uploaded() 写回的仍是扫描时旧 size/mtime
```

下次扫描：

```text
磁盘：新 size/mtime
数据库：旧 size/mtime
```

会再次上传。

这通常只是重复上传，不会静默漏备份，因此安全性还好，但会产生额外版本。

更值得关注的是：

```python
self.store.mark_scan(...)
```

即便：

```text
failed > 0
```

仍然更新 `last_scan`。

所以自动任务失败后，要等完整的下一轮 interval 才会再尝试，而不是快速重试。

例如 24 小时备份周期：

```text
02:00 上传失败
last_scan = 02:00
↓
下一次尝试 24 小时之后
```

更合理的是区分：

```text
last_attempt
last_success
```

失败任务使用退避重试。

------

### 11. 增量备份目录设计会大量复制“变化文件”，但不形成真正快照

`app.py:833-841`：

```python
timestamp = datetime.now().strftime("%Y%m%d-%H%M%S")
prefix = normalize_remote_path(
    self.job.dest_dir,
    timestamp if self.job.mode == "incremental" else "",
)
```

每次增量备份，变化文件进入新的 timestamp 目录。

例如：

```text
20260825-100000/
    A.txt
    B.txt

20260825-120000/
    B.txt

20260825-140000/
    C.txt
```

这三个目录单独看都不是完整快照。

如果 UI 将其描述为“增量备份”，没问题；但恢复某个时间点需要按时间序列合并多个目录。当前代码没有看到 manifest/index 描述：

```text
此增量继承自哪个版本
哪些文件被删除
当前文件集合是什么
```

尤其是**本地删除的文件**：

`backup.py` 会从 `backup_files` 删除 stale 记录：

```python
DELETE FROM backup_files ...
```

但不会在远端记录 tombstone。

因此无法仅凭云端增量目录恢复“某一时间点的准确文件树”。

这是备份语义上的风险，建议至少每次生成：

```json
manifest.json
{
  "base": "...",
  "changed": [...],
  "deleted": [...]
}
```

------

### 12. symlink 问题不仅存在于 Backup，也存在普通目录上传路径

上一轮只指出：

```text
backup.py
```

继续看后发现普通上传也值得统一处理。

`UploadThread._path_size()`：

```python
for child in path.rglob("*"):
    if child.is_file():
        ...
        child.stat()
```

以及大文件筛查：

`service.py`：

```python
candidates = path.rglob("*")
if candidate.is_file():
```

如果目录树中有文件 symlink，Python 文件读取通常会跟随它。

而普通整目录上传的核心：

```python
self.service.upload_folder(...)
```

最终行为还依赖 `modelscope_hub` 对 symlink 的处理。

因此应该建立一个统一规则：

```text
local source root
     ↓ resolve()
所有上传候选
     ↓ resolve()
必须 is_relative_to(root)
```

并明确：

```text
symlink:
[ ] follow
[x] reject
```

对备份工具，我更建议默认 reject。

------

### 13. `repository_is_public()` 使用 Token 存在与否来推断未知 visibility

**等级：中**

`app.py:181-188`：

```python
if visibility in {"public", "5"}:
    return True
if visibility in {"private", "internal", "1", "3"}:
    return False
return not service_token
```

也就是说 visibility 未识别时：

```text
有 Token → private
没 Token → public
```

这是 fail-open/fail-closed 混在一起。

例如 ModelScope SDK 新增了一种 visibility 值，或者接口偶尔返回空字符串：

- Token 账户：判断 private，偏保守；
- 无 Token 服务：判断 public。

对生成公开直链来说，后者是 fail-open。

更稳妥的规则应该是：

```python
UNKNOWN
```

不要直接映射成 public。

特别是：

```python
repository_file_url(...)
```

和图片床 direct URL 依赖这个判断。

建议改成三态：

```python
PUBLIC
PRIVATE
UNKNOWN
```

UNKNOWN 时走需要认证的 API URL，或额外查询。

------

### 14. WebDAV 的多账户路由存在“第一个账户获胜”的隐式权限选择

`service.py`：

```python
for account_id, repos in self.account_repositories.items():
    ...
    for repo in repos:
        self._routes.setdefault((repo.repo_type, repo.repo_id), service)
```

如果两个 Token 都有同一仓库访问权：

```text
account A → owner/repo
account B → owner/repo
```

最终 WebDAV 使用哪个账户完全取决于 dictionary 插入顺序。

对于读取通常没有问题，但对于：

```python
upload_file_as()
```

权限可能不同。

比如：

```text
A = read-only token
B = write token
```

但 A 恰好先插入：

```text
WebDAV GET 正常
WebDAV PUT 全失败
```

甚至未来权限更复杂时可能产生误操作。

建议 route 不应该是：

```python
(repo) -> one service
```

而是：

```python
(repo) -> [candidate services]
```

然后：

- read：任选有效账户；
- write：明确选择具有写能力的 Token；
- UI 可以指定 WebDAV 默认写账户。

------

## 一个很容易被忽略、但我认为应该优先修的问题

### 15. Web Session 使用 `requests.get(... cookies=dict)` 的 Cookie 作用域过宽

`web_session.py` 多处使用：

```python
cookies=session.cookies()
```

其中：

```python
def cookies(self):
    return {
        "m_session_id": ...,
        "csrf_session": ...,
        "csrf_token": ...,
    }
```

这里传入的是普通 dict，而不是带 Domain 属性的 CookieJar。

我针对当前 `requests` 行为做了验证：由普通 dict 创建的 cookie 是：

```text
domain = ''
domain_specified = False
```

这意味着在重定向链中，它不是 `.modelscope.cn` 范围 cookie，而近似于“可发给请求目标”的无域 cookie。

因此与前面的 urllib 问题类似，应该避免：

```python
requests.get(..., cookies=dict)
```

对于认证 Session 更合理的是：

```python
session = requests.Session()

session.cookies.set(
    "m_session_id",
    value,
    domain=".modelscope.cn",
    path="/",
)
```

实际上你在 `ModelScopeWebService.download_to_file()` 已经这么做了：

```python
session.cookies.set(name, value, domain=".modelscope.cn", path="/")
```

这再次说明项目内部有两套不同的凭据边界策略。

这个我会把它与之前的 #3 合并成一个总体问题：

> **所有 ModelScope HTTP 请求缺少统一的 credential-scoping 层。**

这是目前最值得重构的地方之一。

------

## `app.py` 本身的架构问题

现在 `app.py` 是约 **6847 行、313 个成员方法**，里面同时包含：

```text
GUI
账号管理
网页登录
上传
下载
aria2
备份
图片床
媒体播放
播放器安装
WebDAV 生命周期
索引
搜索
远端文件操作
删除
复制/移动
配置
任务调度
系统托盘
```

这已经不仅是“文件有点大”，而是产生了真实风险。

例如：

```python
self.task
```

被复用来表示多个不同类别的 QThread。

然后到处出现：

```python
if self.task and self.task.isRunning():
```

或：

```python
isinstance(self.task, UploadThread)
```

这使“业务互斥关系”隐式分散在 UI 层。

与此同时另外又有：

```python
self.backup_thread
self.image_upload_thread
self.index_task
self.potplayer_install_thread
```

所以现在实际上存在两套任务管理模型。

这也是为什么一些并发边界很难一眼证明正确。

我建议未来不是简单把函数搬文件，而是引入：

```text
MainWindow
   │
   ├── AccountController
   ├── TransferController
   │      ├── UploadManager
   │      └── DownloadManager
   ├── BackupController
   ├── RepositoryController
   ├── WebDavController
   ├── MediaController
   └── IndexController
```

尤其先拆：

```text
TransferController
CredentialHttpClient
BackupController
```

这三个拆分会直接消除几类风险，而不仅仅是改善代码风格。

------

### 目前重新排序后的优先级

我会建议按这个顺序处理：

| 优先级 | 问题                                                         |
| ------ | ------------------------------------------------------------ |
| P0     | 统一 HTTP/redirect credential-scoping，解决 urllib + requests Cookie/Header 跨域问题 |
| P0     | aria2 manifest 明文长期 Token                                |
| P1     | symlink/root containment，覆盖 Backup + 普通上传             |
| P1     | DPAPI machine-scope fallback 明确化或取消                    |
| P1     | SDK `tqdm` / `_CountedReadStream` 全局 monkey-patch          |
| P1     | 备份失败调度和真正的增量恢复语义                             |
| P2     | WebDAV PUT 本机资源配额                                      |
| P2     | MultiAccountService 重复仓库路由                             |
| P2     | unknown visibility 不应推断 public                           |
| P3     | WebDAV HTTP/Basic 本身——在你给出的受控 LAN 约束下不需要专门改 HTTPS |

这里我认为最重要的结论变化是：**无需为了这套 WebDAV 强行引入 HTTPS；应该把工程精力放在“认证信息只在该出现的域/进程/文件中出现”以及“本地数据边界”上。**这比内网自签证书更符合当前威胁模型。





已在你授权的副本目录

```
C:\Users\ARXChem\Documents\Open Source Projects\ModelScpoe-Manager-Repair
```

中直接修改源码，未修改原始 `ModelScpoe-Manager` 目录，也没有清理 `data/`、Token、Cookie、数据库或其他运行数据。

目前已经落地的修复包括：

- **认证信息重定向边界**：新增 `http_security.py`，Token/Cookie 只会主动发送给 ModelScope 官方域；`urllib` 跨 origin 重定向会移除 `Authorization / Cookie / Proxy-Authorization`，同时保留 Range。已接入缩略图、复制下载、WebDAV、媒体代理和普通下载链。
- **网页登录 Cookie 作用域**：`web_session.py` 不再使用 domainless 的 `cookies=dict`；统一建立 `requests.Session`，Cookie 明确绑定 `.modelscope.cn`。在线登录、用户名验证、单文件删除、批量删除和删除后的目录核对接口保持原功能。
- **aria2 Token 落盘问题**：移除了包含 Token 的 `.aria2-input` 临时 manifest。现在 aria2 启动后通过仅监听 `127.0.0.1` 且带随机 RPC secret 的 JSON-RPC `addUri` 注入每个下载任务及其认证头，仍保留分段、断点续传、SHA-256、暂停/恢复和动态限速。
- **上传/备份目录越界**：新增 `local_paths.py`，普通上传和备份都会拒绝 symlink / Windows junction，并在 SDK 读取前验证实际文件仍处于用户选择的根目录内。
- **SDK monkey-patch 并发问题**：上传进度对 `modelscope_hub._upload.tqdm` 的临时替换现在受进程级 `RLock` 保护；普通上传、备份、图床和 WebDAV SDK 写入共享这把锁，避免并发线程互相覆盖全局 `tqdm`。
- **DPAPI 降级问题**：Token 和网页登录 Session 的新保存默认只允许当前用户 DPAPI，不再静默退化到 machine scope；`device.id` 和内网 WebDAV 密码保留兼容性 machine fallback。旧 `m:` 凭据仍能读取，并尝试无损迁移到 user scope；迁移失败不会删除仍可使用的旧凭据。
- **DPAPI 不可用时功能回退**：Token 仍可连接但仅本次运行保存在内存；网页登录 Session 同样可以在当前进程继续用于读取和删除，只是不持久写盘。
- **WebDAV 保持内网设计**：没有引入 HTTPS、自签证书或改变 AList 的 `http://` 挂载方式。增加最多 2 个并发 PUT、本机临时空间预留和至少 512 MiB 空闲空间保护，降低 AList 重试/并发把系统盘临时目录写满的风险。
- **WebDAV 多账户路由**：同一仓库由多个账户可见时，写操作会优先选择真正拥有 Token 的 SDK service，不会误选明确禁止上传的网页登录 service。
- **未知 visibility**：不再因为“没有 Token”就把未知可见性推断成 public；未知状态现在 fail-closed。
- **备份失败调度**：数据库新增 `last_attempt / last_success` 并带兼容迁移。失败不再被标记成一次成功扫描，也不会等完整备份周期才重试，而采用 30 秒～5 分钟退避。
- **备份 TOCTOU**：上传前后都会重新核对 size/mtime；扫描后或上传过程中发生变化的文件不会错误写入“已备份”状态。
- **增量备份恢复**：没有改变 `YYYYMMDD-HHMMSS` 增量目录格式，也没有改变“同路径覆盖”模式。现在利用原有 `backup_files` 索引记录每个当前文件最后成功上传的位置，从多个时间戳增量中重建**当前完整文件集合**；老任务缺少这份映射时仍回退到原来的“最新时间戳目录”逻辑。

当前静态验证状态是：`python -m compileall -q main.py modelscope_manager` **通过**，`git diff --check` **通过**。还做了几个针对性运行验证，包括 DPAPI user-scope 加解密、跨域重定向剥离凭据、网页登录 Cookie domain、symlink 越界拒绝、aria2 RPC 参数生成和数据库迁移。

但这轮还**没有完成最终验收**：当前 Repair 副本的 `tests/` 在我开始修改之前就已经全部处于 Git `D` 状态，所以无法直接运行原来的 100+ 项测试；这些删除不是本次修改造成的，我也没有擅自恢复。此外还没有做真实 ModelScope 的在线登录/删除、真实 aria2 下载、AList WebDAV PUT，以及增量/覆盖备份的远端端到端回归；根目录 README/project 更新也尚未完成。因此目前应视为“主要代码修复已写入并通过语法/静态检查”，而不是已经完成发布级验证。

### 2026-08-25 补充验收

后续检查确认 Repair 副本的 `tests/` 已恢复，但便携 `runtime/` 比原始同版本副本缺少 4037 个文件，导致 `runtime\python.exe` 无法导入 `unittest` 并报告缺少 `pywin32_bootstrap`，这是主程序无法可靠启动的直接部署原因。现已仅补回缺失运行时文件、不覆盖已有文件，并同步更新风险修复后的测试契约；全量 112 项单元测试、`compileall` 和 `git diff --check` 均通过。`runtime\pythonw.exe main.py` 的生产启动链路已显示 `ModelScope Manager` 前台窗口，用户人工确认成功后关闭。真实 ModelScope 写操作、aria2 远端下载、AList PUT 和远端备份恢复仍未执行，继续作为需要授权及真实环境的端到端验证缺口。
